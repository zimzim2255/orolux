<!DOCTYPE html>
<html lang="<%= language %>" dir="<%= isRTL ? 'rtl' : 'ltr' %>">
  
<head>
    <link rel="icon" type="image/svg+xml" href="/public/logo.jpeg">

  <title><%= t('loginPage') %> | OROLUX</title>
  <%- include('partials/head') %>
  <style>
    .login-container {
      min-height: 100vh;
      display: flex;
      align-items: center;
      justify-content: center;
      padding: 2rem;
      background: linear-gradient(135deg, 
        var(--deep-blue) 0%,
        rgba(10, 36, 64, 0.95) 50%,
        var(--gold) 100%
      );
      position: relative;
      overflow: hidden;
    }

    .login-container::before {
      content: '';
      position: absolute;
      top: 0;
      left: 0;
      right: 0;
      bottom: 0;
      background: url('https://images.unsplash.com/photo-1523170335258-f5ed11844a49?ixlib=rb-4.0.3&ixid=M3wxMjA3fDB8MHxwaG90by1wYWdlfHx8fGVufDB8fHx8fA%3D%3D&auto=format&fit=crop&w=1480&q=80') center/cover;
      opacity: 0.1;
      z-index: 0;
    }

    .login-box {
      width: 100%;
      max-width: 500px;
      padding: 4rem;
      background: rgba(255, 255, 255, 0.95);
      border-radius: 4px;
      box-shadow: 0 25px 50px rgba(10, 36, 64, 0.2);
      position: relative;
      z-index: 1;
      backdrop-filter: blur(10px);
    }

    .logo-section {
      text-align: center;
      margin-bottom: 3rem;
    }

    .logo-text {
      font-family: 'Cormorant Garamond', serif;
      font-size: 3rem;
      font-weight: 600;
      color: var(--deep-blue);
      text-decoration: none;
      letter-spacing: 4px;
      display: inline-block;
      padding-bottom: 0.5rem;
      border-bottom: 2px solid var(--gold);
    }

    .login-title {
      font-family: 'Cormorant Garamond', serif;
      font-size: 3.5rem;
      font-weight: 300;
      color: var(--deep-blue);
      margin-bottom: 3rem;
      text-align: center;
      letter-spacing: 2px;
      line-height: 1.2;
    }

    .form-group {
      margin-bottom: 2rem;
    }

    .form-label {
      display: block;
      font-family: 'Montserrat', sans-serif;
      font-size: 1rem;
      color: var(--deep-blue);
      margin-bottom: 0.75rem;
      text-transform: uppercase;
      letter-spacing: 2px;
    }

    .form-input {
      width: 100%;
      padding: 1.25rem;
      border: 2px solid var(--gold);
      border-radius: 4px;
      font-family: 'Montserrat', sans-serif;
      font-size: 1.1rem;
      color: var(--deep-blue);
      background: rgba(255, 255, 255, 0.9);
      transition: all 0.3s ease;
    }

    .form-input:focus {
      outline: none;
      border-color: var(--deep-blue);
      box-shadow: 0 4px 12px rgba(10, 36, 64, 0.1);
      background: white;
    }

    .form-input::placeholder {
      color: rgba(10, 36, 64, 0.5);
    }

    .login-btn {
      width: 100%;
      padding: 1.5rem;
      background: #0a3264; /* Increased blue by 30% */
      color: white;
      border: none;
      border-radius: 4px;
      font-family: 'Montserrat', sans-serif;
      font-size: 1.1rem;
      text-transform: uppercase;
      letter-spacing: 2px;
      cursor: pointer;
      transition: all 0.4s ease;
      margin-top: 2rem;
      position: relative;
      overflow: hidden;
    }

    .login-btn::before {
      content: '';
      position: absolute;
      top: 0;
      left: -100%;
      width: 100%;
      height: 100%;
      background: linear-gradient(
        90deg,
        transparent,
        rgba(255, 255, 255, 0.2),
        transparent
      );
      transition: 0.5s;
    }

    .login-btn:hover {
      background-color: #1a4275; /* Adjusted hover state to match new blue */
      transform: translateY(-3px);
      box-shadow: 0 10px 20px rgba(10, 50, 100, 0.2);
    }

    .login-btn:hover::before {
      left: 100%;
    }

    .register-link {
      margin-top: 3rem;
      text-align: center;
      font-family: 'Montserrat', sans-serif;
      font-size: 1.1rem;
      color: var(--deep-blue);
    }

    .register-link a {
      color: var(--deep-blue);
      text-decoration: none;
      font-weight: 500;
      transition: all 0.3s ease;
      border-bottom: 2px solid var(--gold);
      padding-bottom: 2px;
    }

    .register-link a:hover {
      color: var(--gold);
      border-bottom-color: var(--deep-blue);
    }

    @media (max-width: 768px) {
      .login-container {
        padding: 1rem;
      }

      .login-box {
        padding: 2.5rem;
      }

      .login-title {
        font-size: 2.5rem;
      }

      .logo-text {
        font-size: 2.5rem;
      }
    }
  </style>
</head>
<body class="font-sans antialiased">
  <div class="login-container">
    <div class="login-box">
      <div class="logo-section">
        <a href="/" class="logo-text">OROLUX</a>
      </div>
      
      <h2 class="login-title"><%= t('welcomeBack') %></h2>

      <form action="/login" method="POST">
        <div class="form-group">
          <label class="form-label"><%= t('email') %></label>
          <input type="email" name="email" required class="form-input" 
                 placeholder="<%= t('enterEmail') %>">
        </div>

        <div class="form-group">
          <label class="form-label"><%= t('password') %></label>
          <input type="password" name="password" required class="form-input" 
                 placeholder="<%= t('enterPassword') %>">
        </div>

        <button type="submit" class="login-btn"><%= t('signIn') %></button>
      </form>

      <div class="register-link">
        <p><%= t('noAccount') %> <a href="/register"><%= t('registerNow') %></a></p>
      </div>
    </div>
  </div>

  <%- include('partials/footer') %>
</body>
</html>