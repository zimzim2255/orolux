<!DOCTYPE html>
<html lang="en">
<head>
      <link rel="icon" type="image/jpeg" href="/public/logo.jpeg">

    <title>Error - OROLUX</title>
    <%- include('partials/head') %>
    <style>
        .error-container {
            max-width: 800px;
            margin: 8rem auto 4rem;
            padding: 2rem;
            text-align: center;
        }
        .error-title {
            font-size: 2rem;
            color: #1a365d;
            margin-bottom: 1rem;
        }
        .error-message {
            color: #4a5568;
            margin-bottom: 2rem;
        }
        .back-home {
            display: inline-block;
            padding: 0.75rem 1.5rem;
            background: #1a365d;
            color: white;
            text-decoration: none;
            border-radius: 4px;
            transition: background-color 0.3s;
        }
        .back-home:hover {
            background: #2d4a7c;
        }
    </style>
</head>
<body>
    <%- include('partials/header') %>
    <div class="error-container">
        <h1 class="error-title">Oops! Something went wrong</h1>
        <p class="error-message"><%= message %></p>
        <% if (process.env.NODE_ENV === 'development' && error.stack) { %>
            <pre style="text-align: left; margin: 2rem 0; padding: 1rem; background: #f7fafc; border-radius: 4px;">
                <%= error.stack %>
            </pre>
        <% } %>
        <a href="/" class="back-home">Return to Home</a>
    </div>
    <%- include('partials/footer') %>
</body>
</html>
